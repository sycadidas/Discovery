<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.discovery.dao.AuthUserDao">
    
    <select id="getUserByUserName" parameterType="String" resultType="com.common.dto.User">
        SELECT USER_NAME username,PASSWORD password,AGE age,GENDER gender,ISVIP isVIP FROM USER
        WHERE USER_NAME = #{username}
        AND PASSWORD = #{password}
    </select>
</mapper>